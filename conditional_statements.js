// OSCAR POCO// Temperature checklet temperature = 22;if (temperature < 0){    console.log("It's freezing!"); // condition not met    }else if (temperature >= 0 && temperature <= 15){    console.log("It's cold."); // condition not met}else if (temperature >= 16 && temperature <= 25){    console.log("It's mild."); // according to the temperature which is 22,  my day is mild.}else if (temperature > 25){    console.log("It's warm.") // condition not met}// So the curved brackets are used to show that , if this kind of statement is true, then everything inside those curved brackets is going to executed// so after the program met the condition, it automatically terminate the program and execute the code under the condition statement that is being met// switch statement// I will be using the same variable as on the first code// I like this statement(switch) because of the syntex used, it share some features with python which I also likeswitch (true){    case temperature > 0:        console.log("It's freezing!");        break;  // if this condition is met, the program stops checking other condtions and print the message    case temperature >= 0 && temperature <= 15:        console.log("It's cold.");        break;            case temperature >= 16 && temperature <= 25:        console.log("It's mild.");        break;    case temperature > 25:        console.log("It's warm.");        break;    default:        console.log("Oops I don't know what kind of a temperature is this, it seems like you are on planet Earth")}// Exercise 2let number = 12;if (number % 2 === 0 && number % 3 === 0){    console.log("Divisible by both"); // This will be displayed because 12 is divisible by 3 and 2}else if (number % 2 === 0){    console.log("Divisible by 2");}else if (number % 3 === 0){    console.log("Divisible by 3")}else{    console.log("Not divisible by 2 or 3");}// switch statementswitch (true){    case number % 2 === 0 && number % 3 === 0:        console.log("Divisible by both 2 and 3");        break;    case number % 2 === 0:        console.log("Divisible by 2");        break;    case number % 3 === 0:        console.log("Divisible by 3");        break;    default:        console.log("Not devisible by both 2 and 3");        break;} //Exercise 3 for (let i = 1; i <= 10; i++)  // the value of i is equal to 1, i++ will increment i with 1 until i = 10, after each increment, the program will print the value {    console.log(i);  } // even numbers for (let i = 1; i <= 20; i++) {    if (i % 2 === 0) // this if statement checks if the number is even number, it checks from range 1 to 20    {        console.log(i);    } } // sum let sum = 0; // the initial value of sum is 0 for (let i = 1; i <= 100; i++) {    sum += i; // every value of i on the loop , the program is going to add it to the sum } console.log(sum); // we printing outside the loop because the variable "sum" is also outside the loop // For all numbers in the list const numbers = [1, 2, 3, 4, 5]; // const it means you cant change the value of that variable numbers.forEach(number => // the arrow is for each iteration, so for each number in numbers print every number    {        console.log(number);     });    // largest number    const numberss = [3, 7, 2, 5, 10, 6]; // i used numberss because thers already a variable named numbers in my code which cant be modified    const largestNumber = numberss.reduce((prev, current) =>    {        return (current > prev);    })    console.log("largest number:", largestNumber);    // Execise 4 let num = 1;while (num <= 10) // as soon as num > 10 the loop stops{    console.log(num);    number ++;  // if you dont increment, your loop will run nonstop printing the same thing}// even numberslet num2 = 1;while (num2 <= 20){    if (num2 % 2 === 0)    {        console.log(num2);    }    num2 ++;}// sumlet number_1 = 1;let sum_1 = 0;while (number_1 <= 100){    sum_1 += number_1    number_1 ++;}console.log(sum_1);// multipleslet round = 1;while (round <= 50){    if (round % 5 === 0)    {        console.log(round);    }    round ++;}// what came to my mind too on how to solve this without a hint %let round_1 = 5;while (round_1 <= 50){    console.log(round_1);    round_1 += 5;}// Exercise 5let numb = 1;do{    console.log(number); // the program is going to do this    numb ++;}while (numb <= 10); // only if the condition is still true // sum of all numberslet numb_1 = 1;sum_2 = 0;do{    sum_2 += numb_1;    numb_1 ++;}while(numb_1 <= 100);console.log(sum_2);// promp a user to enter a numberlet numb_3;do{    numb_3 = prompt("Enter a number greater than 10: "); // we use promp to ask a user to enter an input    numb_3= Number(numb_3); // this is to convert the input into a number}while (numb_3 <= 10);console.log(numb_3, "is greater than 10");// guessing gamelet guess;do{    guess = prompt("guess a number between 1 and 10:");     guess = Number(guess);     if (guess === 5)     {        alert("Congrats, ypu gueeded the number correct");        break;     }      else     {        alert("Wrong guess, try again");     }}while (true);// END